# Source_Files/Misc/CourierPrimeItalic.h

## File Purpose
Binary font resource file embedding the Courier Prime Italic typeface as a C array. This header allows the font to be compiled directly into applications without requiring external font files at runtime.

## Core Responsibilities
- Stores complete TrueType/OpenType font binary data
- Provides static font resource for embedded use in C/C++ applications
- Eliminates runtime font file dependencies

## Key Types / Data Structures

| Name | Kind | Purpose |
|------|------|---------|
| `courier_prime_italic` | unsigned char array | Raw binary font data containing glyph metrics, outlines, and licensing metadata |

## Global / File-Static State

| Name | Type | Scope | Purpose |
|------|------|-------|---------|
| `courier_prime_italic` | `unsigned char[]` | global | Statically allocated font binary resource (~12KB) |

## Key Functions / Methods
NoneΓÇöthis is a data-only resource file.

## Control Flow Notes
Not applicable. This is a static resource with no execution flow. Font rendering engines (FreeType, DirectWrite, etc.) consume this binary data at initialization time.

## External Dependencies
- None directly. The array is consumed by font rendering libraries that understand TTF/OTF binary format.
- **Embedded data includes** font table structures (OS/2, glyf, cmap, etc.) and legal/licensing text in UTF-16 encoding.

## Notes
- Generated by `/home/ghs/bin/bin2h.py` on 2013-06-17
- Font contains full Latin character set with diacritical marks, arrows, currency symbols, and other Unicode glyphs
- Includes OpenType licensing information embedded in the `name` table
- Array is complete and ready for direct `memcpy()` or file-like access by font libraries
